# Builds
dist
build
*.min.js

# Vector graphics
*.svg

# Dependencies
node_modules

# Addons should follow the rules that its upstream follows.
/packages/gui/src/addons/*

# Settings is not an addon
!/packages/gui/src/addons/settings

# Whatever the skibidi this is.
.nyc_output

# package-lock should not be formatted, but it sho uld be committed. ;)
package-lock.json

# GitHub Actions workflwows.
# We don't use these often anymore; we now use Codeberg's CI.
.github/workflows

# this might break again
packages/blocks

# for some reason prettier keeps attempting to write into autogenerated desktop
# files which crashes it
packages/desktop